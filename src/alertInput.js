import React from 'react';
import App from './App';
// import onInputChange from './oneInputChange';
// import render from './render';

const alertInput = (login,password) => {
    alert(`${login} ${password}`);
  };

export default alertInput;